"use client";

import React, { useState } from "react";
import { BookOpen, Search, Sparkles, Cpu, Zap, BatteryCharging, Check } from "lucide-react";
import Link from "next/link";

interface Formula {
  id: string;
  name: string;
  expression: string;
  category: "Basic" | "Machines" | "Power Systems" | "Electronics";
  description: string;
  parameters: string[];
}

export default function FormulaLibrary() {
  const [searchQuery, setSearchQuery] = useState("");
  const [copiedId, setCopiedId] = useState<string | null>(null);

  const formulas: Formula[] = [
    {
      id: "ohms_law",
      name: "Ohm's Law",
      expression: "V = I x R",
      category: "Basic",
      description: "Relates the voltage drop across a linear conductor to the current flowing through it and its resistance.",
      parameters: ["V: Voltage (V)", "I: Current (A)", "R: Resistance (Ω)"]
    },
    {
      id: "kcl",
      name: "Kirchhoff's Current Law (KCL)",
      expression: "∑ I_in = ∑ I_out",
      category: "Basic",
      description: "Based on the conservation of charge, the sum of currents entering any node equals the sum of currents leaving it.",
      parameters: ["I: Currents at the node boundary"]
    },
    {
      id: "kvl",
      name: "Kirchhoff's Voltage Law (KVL)",
      expression: "∑ V_drops = 0",
      category: "Basic",
      description: "Based on the conservation of energy, the sum of electrical potential differences (voltage) around any closed loop is zero.",
      parameters: ["V: Voltage potentials and source EMFs"]
    },
    {
      id: "synchronous_speed",
      name: "Synchronous Speed",
      expression: "Ns = 120 x f / P",
      category: "Machines",
      description: "Calculates the rotation speed of the magnetic field generated by the stator windings of an AC machine.",
      parameters: ["Ns: Synchronous speed (RPM)", "f: Supply frequency (Hz)", "P: Number of machine poles"]
    },
    {
      id: "induction_slip",
      name: "Induction Motor Slip",
      expression: "s = (Ns - Nr) / Ns",
      category: "Machines",
      description: "Defines the relative difference between the synchronous stator field speed and the actual rotor mechanical speed.",
      parameters: ["s: Slip (fraction or %)", "Ns: Synchronous speed (RPM)", "Nr: Rotor mechanical speed (RPM)"]
    },
    {
      id: "transformer_turns",
      name: "Transformer Turns Ratio",
      expression: "a = N1 / N2 = V1 / V2 = I2 / I1",
      category: "Machines",
      description: "Defines the ratio of primary windings to secondary windings, dictating step-up or step-down voltage conversions.",
      parameters: ["a: Turns ratio", "N1, N2: Number of turns", "V1, V2: Terminal voltages"]
    },
    {
      id: "buck_chopper",
      name: "Buck Converter Output",
      expression: "Vout = D x Vin",
      category: "Power Systems",
      description: "Calculates the average output voltage of a step-down DC-to-DC chopper converter operating in continuous conduction.",
      parameters: ["Vout: Output voltage (V)", "D: Duty cycle (0.0 to 1.0)", "Vin: DC input source (V)"]
    },
    {
      id: "boost_chopper",
      name: "Boost Converter Output",
      expression: "Vout = Vin / (1 - D)",
      category: "Power Systems",
      description: "Calculates the output voltage of a step-up DC-to-DC chopper converter, scaling values higher than the input source.",
      parameters: ["Vout: Output voltage (V)", "Vin: DC input source (V)", "D: Duty cycle"]
    },
    {
      id: "opamp_gain",
      name: "Non-Inverting Op-Amp Gain",
      expression: "Av = 1 + (Rf / R1)",
      category: "Electronics",
      description: "Determines the closed-loop voltage amplification factor of a standard operational amplifier in non-inverting mode.",
      parameters: ["Av: Voltage gain", "Rf: Feedback resistance (Ω)", "R1: Input resistor (Ω)"]
    }
  ];

  const handleCopyExpression = (f: Formula) => {
    navigator.clipboard.writeText(f.expression);
    setCopiedId(f.id);
    setTimeout(() => setCopiedId(null), 1500);
  };

  const filteredFormulas = formulas.filter(f =>
    f.name.toLowerCase().includes(searchQuery.toLowerCase()) ||
    f.category.toLowerCase().includes(searchQuery.toLowerCase()) ||
    f.description.toLowerCase().includes(searchQuery.toLowerCase())
  );

  return (
    <div className="flex-1 bg-slate-50 dark:bg-slate-950 text-slate-900 dark:text-slate-100 p-6 overflow-y-auto">
      <div className="max-w-5xl mx-auto space-y-6">
        
        {/* Header */}
        <div className="flex flex-col md:flex-row md:items-center md:justify-between border-b pb-5 border-slate-200 dark:border-slate-800 gap-4">
          <div>
            <h1 className="text-2xl font-extrabold text-slate-950 dark:text-white flex items-center space-x-2">
              <BookOpen className="h-6 w-6 text-blue-600" />
              <span>Formula & Knowledge Library</span>
            </h1>
            <p className="text-xs text-slate-400 mt-1">Reference database of standard electrical engineering equations.</p>
          </div>
          
          {/* Search Box */}
          <div className="relative w-full md:w-80">
            <Search className="absolute left-3 top-2.5 h-4.5 w-4.5 text-slate-400" />
            <input
              type="text"
              placeholder="Search formulas or concepts..."
              value={searchQuery}
              onChange={(e) => setSearchQuery(e.target.value)}
              className="w-full pl-10 pr-4 py-2 text-xs rounded-xl border border-slate-200 dark:border-slate-800 bg-white dark:bg-slate-900 text-slate-900 dark:text-slate-100 font-medium focus:ring-2 focus:ring-blue-500 focus:outline-none"
            />
          </div>
        </div>

        {/* Categories Quick Filter */}
        <div className="flex flex-wrap gap-2">
          {["All", "Basic", "Machines", "Power Systems", "Electronics"].map((cat) => (
            <button
              key={cat}
              onClick={() => setSearchQuery(cat === "All" ? "" : cat)}
              className="px-3 py-1.5 rounded-full border border-slate-250 dark:border-slate-800 bg-white dark:bg-slate-900 hover:bg-slate-50 dark:hover:bg-slate-800 text-[11px] font-semibold transition-colors"
            >
              {cat}
            </button>
          ))}
        </div>

        {/* Formulas Grid */}
        <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
          {filteredFormulas.map((f) => (
            <div
              key={f.id}
              className="p-5 bg-white dark:bg-slate-900 rounded-2xl border border-slate-200/60 dark:border-slate-800 flex flex-col space-y-4 hover:shadow-md transition-shadow"
            >
              {/* Top Row */}
              <div className="flex justify-between items-start">
                <div>
                  <span className={`text-[10px] px-2.5 py-0.5 rounded-full font-bold ${
                    f.category === "Basic" ? "bg-blue-100 text-blue-800" :
                    f.category === "Machines" ? "bg-indigo-100 text-indigo-800" :
                    f.category === "Power Systems" ? "bg-emerald-100 text-emerald-800" : "bg-purple-100 text-purple-800"
                  }`}>
                    {f.category}
                  </span>
                  <h3 className="font-bold text-sm text-slate-950 dark:text-white mt-2">{f.name}</h3>
                </div>
                
                <button
                  onClick={() => handleCopyExpression(f)}
                  className="p-1 rounded text-slate-400 hover:text-slate-600 dark:hover:text-slate-200 transition-colors"
                  title="Copy Expression"
                >
                  {copiedId === f.id ? <Check className="h-4 w-4 text-emerald-500" /> : <BookOpen className="h-4 w-4" />}
                </button>
              </div>

              {/* Expression Render */}
              <div className="p-4 bg-slate-50 dark:bg-slate-950 border border-slate-100 dark:border-slate-850 rounded-xl text-center font-serif text-lg text-blue-600 dark:text-blue-400 font-bold select-all">
                {f.expression}
              </div>

              {/* Description & Parameters */}
              <div className="text-xs text-slate-500 space-y-2 flex-1">
                <p>{f.description}</p>
                <div className="border-t border-slate-100 dark:border-slate-800 pt-2">
                  <span className="font-bold block text-[10px] text-slate-400 uppercase tracking-wider mb-1">Variable mapping:</span>
                  <div className="grid grid-cols-2 gap-1 font-mono text-[10px]">
                    {f.parameters.map((p, i) => (
                      <span key={i} className="text-slate-600 dark:text-slate-400">{p}</span>
                    ))}
                  </div>
                </div>
              </div>

              {/* Explain with AI Action */}
              <div className="border-t border-slate-100 dark:border-slate-800 pt-3 flex justify-end">
                <Link
                  href="/ai-assistant"
                  className="flex items-center space-x-1 text-[11px] font-bold text-purple-600 dark:text-purple-400 hover:underline"
                >
                  <Sparkles className="h-3 w-3" />
                  <span>Explain Derivation with AI</span>
                </Link>
              </div>
            </div>
          ))}
        </div>

        {filteredFormulas.length === 0 && (
          <div className="text-center py-12 text-slate-400 italic">
            No formulas found matching your filter query.
          </div>
        )}

      </div>
    </div>
  );
}
